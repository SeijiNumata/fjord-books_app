 <%= link_to '本一覧へ', books_path %>
 
 <table>
  <thead>
    <tr>
      <th>ユーザー名</th>
    </tr>
     </thead>

  <tbody>
  <% @users.each do |user| %>
   <tr>
    <td><%= user.user_name %></td>
    <td><%= link_to '詳細', user %></td>
    </tr>
  <% end %>
    </tbody>
</table>

<%= paginate @users %>
